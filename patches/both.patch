diff --git a/node_modules/@rocicorp/zero/.bun-tag-29474924caad38a0 b/.bun-tag-29474924caad38a0
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/out/zql/src/ivm/take.js b/out/zql/src/ivm/take.js
index c3b489c5bd79e86074b781beb61232529aa87605..ba1b6ee648fdbb3c7cdabc9ddea53e65edf4e5ad 100644
--- a/out/zql/src/ivm/take.js
+++ b/out/zql/src/ivm/take.js
@@ -237,7 +237,17 @@ var Take = class {
 		assert(!this.#partitionKeyComparator || this.#partitionKeyComparator(change[2].row, change[1].row) === 0, "Unexpected change of partition key");
 		const { takeState, takeStateKey, maxBound, constraint } = this.#getStateAndConstraint(change[2].row);
 		if (!takeState) return;
-		assert(takeState.bound, "Bound should be set");
+		if (takeState.bound === void 0) {
+			// The take window is empty (size 0). A Skip (a `.start()` cursor)
+			// forwards an edit whenever the old and new rows both sort past its
+			// bound, and an input can deliver such an edit while this take holds
+			// no rows. The add-, remove-, and child-change branches all tolerate
+			// an undefined bound; the edit branch must too. The window has room
+			// (size 0 < limit), so surface the new row as an add.
+			this.#setTakeState(takeStateKey, takeState.size + 1, change[1].row, maxBound);
+			yield* this.#output.push(makeAddChange(change[1]), this);
+			return;
+		}
 		const { compareRows } = this.getSchema();
 		const oldCmp = compareRows(change[2].row, takeState.bound);
 		const newCmp = compareRows(change[1].row, takeState.bound);
diff --git a/out/zqlite/src/query-builder.js b/out/zqlite/src/query-builder.js
index 7918d214d218ce67e4ff5ee202554b53884560d2..dcb0bbf6fd94b673f821636de41cbd680c7e860e 100644
--- a/out/zqlite/src/query-builder.js
+++ b/out/zqlite/src/query-builder.js
@@ -63,9 +63,18 @@ function toSQLiteType(v, type) {
 	}
 }
 function nullableAwareEquality(field, value, columnType) {
+	// A NULL bound value proves the column is nullable regardless of the
+	// column metadata, and `=` never matches NULL.
+	if (value === null) return sql`${sql.ident(field)} IS ${value}`;
 	return columnType.optional === true ? sql`${sql.ident(field)} IS ${value}` : sql`${sql.ident(field)} = ${value}`;
 }
 function nullableAwareRangeComparison(field, value, operator, columnType) {
+	// A NULL bound value proves the column is nullable. SQLite sorts NULLs
+	// first: strictly after a NULL bound = exactly the non-NULL values;
+	// strictly before it = nothing.
+	if (value === null) {
+		return operator === ">" ? sql`${sql.ident(field)} IS NOT NULL` : sql`FALSE`;
+	}
 	const comparison = sql`${sql.ident(field)} ${sql.__dangerous__rawValue(operator)} ${value}`;
 	if (columnType.optional !== true) return comparison;
 	return operator === ">" ? sql`(${value} IS NULL OR ${comparison})` : sql`(${sql.ident(field)} IS NULL OR ${comparison})`;
